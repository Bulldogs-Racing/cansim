{
  "name": "canlab-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "CanLab visual editor shell (Phase 5 — placeholder canvas; simulation stays in the Rust engine)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "test": "echo 'frontend tests land with the Phase 5 editor' && exit 0"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "reactflow": "^11.11.4"
  },
  "devDependencies": {
    "@types/react": "^18.3.31",
    "@types/react-dom": "^18.3.7",
    "typescript": "^5.9.3",
    "vite": "^5.4.21"
  }
}
